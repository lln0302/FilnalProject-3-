<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.myapp.gather.GatherDAO">
	<insert id="gatherInsert">
		INSERT INTO gather(nickname, title, content, place, startdate, enddate, gmemberno)
		VALUES(#{nickname}, #{title}, #{content}, #{place},
		STR_TO_DATE(#{startdate}, '%Y-%m-%d'),  STR_TO_DATE(#{enddate}, '%Y-%m-%d'), 
		#{gmemberno})
	</insert>
	
</mapper>